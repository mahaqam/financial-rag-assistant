# Data

This project uses the FinQA dataset. The current verified benchmark was run on the uploaded `dev.json` split containing 883 questions. Raw data is intentionally not committed. Place `dev.json` under `data/` and run the scripts against that path.
